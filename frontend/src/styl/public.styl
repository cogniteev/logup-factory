@import 'nib'
@import 'mrmrscolors'
@import 'brandcolors'

maxWidth = 1200px

*
  box-sizing border-box

.container
  display flex
  flex-direction column
  min-height 100%

nav
  padding 1rem
  >*
    margin 0 auto
    max-width maxWidth

main
  display flex
  flex 1
  flex-direction column
  >div.centered
    align-items center
    display flex
    flex 1
    flex-direction column
    justify-content center
    >div
      max-width 400px
      width 90%
      .form-title
        margin 0
        text-align center
      form
        display flex
        flex-direction column
        margin-top 10px
        >*
          margin-top 10px
          &:first-child
            margin-top 0
      .sub
        margin-top 10px
        text-align center

footer
  padding 10px
  text-align center
